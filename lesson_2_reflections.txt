What happens when you initialize a repository? Why do you need to do it?
-The git directory is added to the working directory.

How is the staging area different from the working directory and the repository?
What value do you think it offers?
-The staging area acts as intermediary between the working directory and the repository.

How can you use the staging area to make sure you have one commit per logical
change?
-by adding all files first before commiting.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
- Branches help out in trying other parts of your initial codes wuthout having to editing the existing one.

How do the diagrams help you visualize the branch structure?
- It made it for more understable by seeing the actual movement of the tree.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
-Merging two branches together creates a branch with both sets of commits in it.
-We represent in the diagram we do so that we can understand it better because we can visualize it.

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?

pros
- It requires lesser time when git merges automatically.
- No merging conflicts involved.

cons
- you cant change the edit label when git does it automatically.
